<h1> All articles</h1>

<% @articles.each do |article| %>
<h2> <%= article.title %> • <%= link_to("Delete", article_path(article), method: :delete, data: { confirm: "Are you sure? 🤨" }) %></h2>
<% end %>
